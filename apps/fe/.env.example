# TODO(template): set the deployed frontend URL in production.
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Required HTTP(S) API base; cross-origin APIs must allow NEXT_PUBLIC_APP_URL via CORS.
NEXT_PUBLIC_API_URL="http://localhost:3001"

# Optional server-only API address, e.g. a Docker service name.
# API_URL="http://be:3001"
