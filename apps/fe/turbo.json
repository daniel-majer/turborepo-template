{
  "$schema": "https://turborepo.dev/schema.json",
  "extends": ["//"],
  "tasks": {
    "check-types": {
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/types/**", "next-env.d.ts", "tsconfig.tsbuildinfo"]
    },
    "test": {
      "dependsOn": ["transit", "^generate"]
    }
  }
}
